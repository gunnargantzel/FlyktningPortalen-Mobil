# Azure Web App Deployment Configuration
# This file configures deployment settings for Azure App Service

version: 1.0.0
name: FlyktningPortalen-Mobil

# Build configuration
build:
  commands:
    - echo "Building PWA..."
    - echo "No build process needed for static files"

# Deploy configuration  
deploy:
  commands:
    - echo "Deploying PWA files..."
    - echo "All files are ready for deployment"

# Runtime configuration
runtime:
  node_version: "18"
  
# Environment variables
env:
  NODE_ENV: "production"
  WEBSITE_NODE_DEFAULT_VERSION: "18"
